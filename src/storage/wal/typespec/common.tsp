// Shared WAL TypeSpec aliases.
//
// tsc-py preserves unknown TypeSpec references in the generated C++ field text.
// The WAL templates map these aliases to the Ladybug C++ types and helpers.

model WalRecordBase {}
